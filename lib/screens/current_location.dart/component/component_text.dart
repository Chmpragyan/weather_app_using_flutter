import 'package:flutter/material.dart';

class CustomCityName extends StatelessWidget {
  const CustomCityName({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}